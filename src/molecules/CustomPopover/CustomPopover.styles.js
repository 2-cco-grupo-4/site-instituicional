import { makeStyles } from '@mui/styles'

export default makeStyles(() => ({
  teste: {
    backgroundColor: '#000',
    color: '#fff',
  },
  popover: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    marginLeft: '-60px'
  },
}))