import { makeStyles } from '@mui/styles'

export default makeStyles(() => ({
  teste: {
    backgroundColor: '#000',
    color: '#fff',
  },
  iconFunil: {
    position: 'relative',
    right: 0,
    fontSize: '30px'
}
}))